# Python-Log-Anonymizer-for-IP-and-Email-Removal
This repository demonstrates a Python based log anonymizer that scans log or text files and removes sensitive data such as IP addresses and email addresses. It uses regular expressions, file handling, and pattern matching to sanitize logs while preserving their structure and readability.
